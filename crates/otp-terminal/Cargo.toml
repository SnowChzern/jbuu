[package]
name = "otp-terminal"
description = "OTP 终端协议：PTY 终端会话（单主 lease、窗口/退出码、恢复新签发段）"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
otp-types.workspace = true
otp-codec.workspace = true
otp-handshake.workspace = true
otp-session.workspace = true
otp-transport.workspace = true
# PTY/termios syscall 集中在平台层（任务 #54 硬边界：libc 调用遵循
# otp-platform 既有 deny+逐点 allow 模式）；本 crate 保持 forbid(unsafe)。
otp-platform.workspace = true
